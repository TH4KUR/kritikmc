import React from "react";

const Timer = (type) => {
  const deadline = new Date("2024-06-24T00:00:00");
  const timestamp = deadline - Date.now();
  const hours = date / 100 / 60 / 60;
  const mins = date / 100 / 60;

  return (
    <div>
      <p>Hurry Up! Registrations Close in: {}</p>
    </div>
  );
};

export default Timer;
